package {{ .PackageName }};

import net.stef.BytesWriter;
import net.stef.SizeLimiter;
import net.stef.WriteColumnSet;
import net.stef.codecs.*;

import java.io.IOException;

// Encoder for {{ .MultimapName }}
public class {{ .MultimapName }}Encoder {
    private BytesWriter buf;
    private WriteColumnSet columns;
    private SizeLimiter limiter;

    private {{.Key.Type.EncoderType}}Encoder keyEncoder;
    private {{.Value.Type.EncoderType}}Encoder valueEncoder;

    private {{ .MultimapName }} lastVal = new {{ .MultimapName }}();

    public void init(WriterState state, WriteColumnSet columns) throws IOException {
        this.limiter = state.getLimiter();
    {{- if .Key.Recursive}}
        keyEncoder = state.{{.Key.Type.EncoderType}}Encoder;
    {{- else}}
        keyEncoder = new {{.Key.Type.EncoderType}}Encoder();
    {{- if .Key.Type.IsPrimitive}}
        keyEncoder.init({{if .Key.Type.DictName}}state.{{.Key.Type.DictName}}{{else}}null{{end}}, limiter, columns.addSubColumn());
    {{- else}}
        keyEncoder.init(state, columns.addSubColumn());
    {{- end}}
    {{- end}}

    {{- if .Value.Recursive}}
        valueEncoder = state.{{.Value.Type.EncoderType}}Encoder;
    {{- else}}
        valueEncoder = new {{.Value.Type.EncoderType}}Encoder();
    {{- if .Value.Type.IsPrimitive}}
        valueEncoder.init({{if .Value.Type.DictName}}&state.{{.Value.Type.DictName}}{{else}}null{{end}}, limiter, columns.addSubColumn());
    {{- else}}
        valueEncoder.init(state, columns.addSubColumn());
    {{- end}}
    {{- end}}
    }

    public void reset() {
    {{ if not .Key.Recursive}}
        keyEncoder.reset();
    {{- end}}
    {{ if not .Value.Recursive}}
        valueEncoder.reset();
    {{end -}}
    }

    // IsEqual performs deep comparison and returns true if encoder's previously encoded value is equal to list.
    public boolean isEqual({{ .MultimapName }} list) {
        return lastVal.isEqual(list);
    }

    public boolean encode({{ .MultimapName }} list) {
        // Implement encoding logic as in Go template
        return false;
    }

    private boolean encodeValuesOnly({{ .MultimapName }} list) {
        // Implement value-only encoding logic
        return false;
    }

    private void encodeFull({{ .MultimapName }} list) {
        // Implement full encoding logic
    }

    public void rencodeLast() {
        // Implement re-encoding of last value
    }

    public void collectColumns(WriteColumnSet columnSet) {
        // Implement column collection logic
    }
}

