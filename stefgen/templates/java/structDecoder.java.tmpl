// {{ .StructName }}Decoder implements decoding of {{ .StructName }}
package {{ .PackageName }};

import net.stef.BitsReader;
import net.stef.ReadColumnSet;
import net.stef.ReadableColumn;

public class {{ .StructName }}Decoder {
    private BitsReader buf = new BitsReader();
    private ReadableColumn column;
    private {{.StructName}} lastValPtr;
    private {{.StructName}} lastVal = new {{.StructName}}();
    private int fieldCount;

    {{ range .Fields }}
    private {{ .Type.EncoderType }}Decoder {{.name}}Decoder = new {{ .Type.EncoderType }}Decoder();
    {{- end }}
    {{if .DictName}}
    private {{ .StructName }}DecoderDict dict;
    {{end}}

    // Init is called once in the lifetime of the stream.
    public void init(ReaderState state, ReadColumnSet columns) throws Exception {
        state.set{{ .StructName }}Decoder(this);
        if (state.getOverrideSchema() != null) {
            int fieldCount = state.getOverrideSchema().fieldCount("{{ .StructName }}");
            this.fieldCount = fieldCount;
        } else {
            this.fieldCount = {{len .Fields}};
        }
        this.column = columns.getColumn();
        {{if .IsMainStruct}}
        this.lastVal.init();
        {{- else}}
        this.lastVal.init(null, 0);
        {{- end}}
        this.lastValPtr = this.lastVal;
        {{- if .DictName}}
        this.dict = state.get{{.DictName}}();
        {{- end}}
        Exception err = null;
        {{ range $i,$e := .Fields }}
        if (this.fieldCount <= {{$i}}) {
            return; // {{.Name}} and subsequent fields are skipped.
        }
        {{- if .Type.IsPrimitive}}
            {{- if .Type.DictName}}
            this.{{.name}}Decoder.init(state.get{{.Type.DictName}}(), columns.addSubColumn());
            {{- else if .Type.IsDictPossible}}
            this.{{.name}}Decoder.init(null, columns.addSubColumn());
            {{- else}}
            this.{{.name}}Decoder.init(columns.addSubColumn());
            {{- end}}
        {{- else}}
        this.{{.name}}Decoder.init(state, columns.addSubColumn());
        {{- end}}
        if (err != null) {
            throw err;
        }
        {{- end }}
    }

    // Continue is called at the start of the frame to continue decoding column data.
    // This should set the decoder's source buffer, so the new decoding continues from
    // the supplied column data. This should NOT reset the internal state of the decoder,
    // since columns can cross frame boundaries and the new column data is considered
    // continuation of that same column in the previous frame.
    public void cont() {
        this.buf.reset(this.column.getData());
        {{ range $i,$e := .Fields }}
        if (this.fieldCount <= {{$i}}) {
            return; // {{.Name}} and subsequent fields are skipped.
        }
        this.{{.name}}Decoder.cont();
        {{- end }}
    }

    public void reset() {
        {{- range .Fields}}
        this.{{.name}}Decoder.reset();
        {{- end}}
    }

    public void decode({{if .DictName}}{{.StructName}}[] dstPtr{{else}}{{.StructName}} dstPtr{{end}}) throws Exception {
        {{- if .DictName}}
        // Check if the {{ .StructName }} exists in the dictionary.
        boolean dictFlag = this.buf.readBit();
        if (!dictFlag) {
            long refNum = this.buf.readUvarintCompact();
            if (refNum >= this.dict.size()) {
                throw new Exception("Invalid refNum");
            }
            this.lastValPtr = this.dict.getByIndex((int)refNum);
            dstPtr[0] = this.lastValPtr;
            return;
        }
        // lastValPtr here is pointing to an element in the dictionary. We are not allowed
        // to modify it. Make a clone of it and decode into the clone.
        {{.StructName}} val = this.lastValPtr.clone();
        this.lastValPtr = val;
        dstPtr[0] = val;
        {{- else}}
        {{.StructName}} val = dstPtr;
        {{- end}}
        // Read bits that indicate which fields follow.
        val.getModifiedFields().mask = this.buf.readBits(this.fieldCount);
        {{ if .OptionalFieldCount}}
        // Write bits to indicate which optional fields are set.
        val.setOptionalFieldsPresent(this.buf.readBits({{ .OptionalFieldCount}}));
        {{- end}}
        {{ range .Fields }}
        if ((val.getModifiedFields().mask & {{ $.StructName }}.fieldModified{{.Name}}) != 0
            {{- if .Optional}} && (val.getOptionalFieldsPresent() & fieldPresent{{ $.StructName }}{{.Name}}) != 0{{end}}) {
            // Field is changed and is present, decode it.
            this.{{.name}}Decoder.decode(val.get{{.Name}}());
        }
        {{ end }}
        {{if .DictName}}
        this.dict.add(val);
        {{end}}
    }
}

