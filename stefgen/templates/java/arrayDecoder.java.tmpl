package {{ .PackageName }};

// Decoder for {{ .ArrayName }}
public class {{ .ArrayName }}Decoder {
    private BitsReader buf = new BitsReader();
    private ReadableColumn column;
    private {{if .Recursive}} {{.ElemType.EncoderType}}Decoder {{else}} {{.ElemType.EncoderType}}Decoder decoder = new {{.ElemType.EncoderType}}Decoder(); {{end}}
    private int prevLen = 0;
    private {{.ElemType.Storage}} lastVal;
    private {{.ElemType.Storage}} lastValPtr;

    // Init is called once in the lifetime of the stream.
    public void init(ReaderState state, ReadColumnSet columns) throws Exception {
        column = columns.column();
        {{if .ElemType.IsPrimitive}}
        {{if .ElemType.DictName}}
        decoder.init(state.{{.ElemType.DictName}}, columns.addSubColumn());
        {{else if .ElemType.IsDictPossible}}
        decoder.init(null, columns.addSubColumn());
        {{else}}
        decoder.init(columns.addSubColumn());
        {{end}}
        {{else}}
        {{if .Recursive}}
        decoder = state.{{.ElemType.EncoderType}}Decoder;
        {{else}}
        decoder.init(state, columns.addSubColumn());
        state.{{.ElemType.EncoderType}}Decoder = decoder;
        {{end}}
        lastVal = new {{.ElemType.Storage}}();
        lastVal.init(null, 0);
        {{end}}
        lastValPtr = lastVal;
    }

    // Continue is called at the start of the frame to continue decoding column data.
    // This should set the decoder's source buffer, so the new decoding continues from
    // the supplied column data. This should NOT reset the internal state of the decoder,
    // since columns can cross frame boundaries and the new column data is considered
    // continuation of that same column in the previous frame.
    public void cont() {
        buf.reset(column.data());
        {{if not .Recursive}}
        decoder.cont();
        {{end}}
    }

    public void reset() {
        prevLen = 0;
        {{if not .Recursive}}
        decoder.reset();
        {{end}}
    }

    public void decode({{.ArrayName}} dst) throws Exception {
        int lenDelta = buf.readVarintCompact();
        int newLen = prevLen + lenDelta;
        dst.ensureLen(newLen);
        prevLen = newLen;
        for (int i = 0; i < newLen; i++) {
            {{if .ElemType.IsPrimitive}}
            lastVal = decoder.decode();
            dst.elems.set(i, lastVal);
            {{else}}
            decoder.decode(lastValPtr);
            dst.elems.get(i).copyFrom(lastValPtr);
            {{end}}
        }
    }
}

