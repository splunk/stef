// Code generated by stefc. DO NOT EDIT.
package com.example.otelstef;

import net.stef.BitsReader;
import net.stef.ReadColumnSet;
import net.stef.ReadableColumn;
import net.stef.codecs.*;

import java.io.IOException;

// Decoder for Float64Array
class Float64ArrayDecoder {
    private final BitsReader buf = new BitsReader();
    private ReadableColumn column;
    private Float64Decoder elemDecoder;
    private boolean isRecursive = false;

    // init is called once in the lifetime of the stream.
    public void init(ReaderState state, ReadColumnSet columns) throws IOException {
        column = columns.getColumn();
        elemDecoder = new Float64Decoder();
        elemDecoder.init(columns.addSubColumn());
    }

    // continueDecoding is called at the start of the frame to continue decoding column data.
    // This should set the decoder's source buffer, so the new decoding continues from
    // the supplied column data. This should NOT reset the internal state of the decoder,
    // since columns can cross frame boundaries and the new column data is considered
    // continuation of that same column in the previous frame.
    public void continueDecoding() {
        buf.reset(column.getData());
        if (!isRecursive) {
            elemDecoder.continueDecoding();
        }
    }

    public void reset() {
        if (!isRecursive) {
            elemDecoder.reset();
        }
    }

    public Float64Array decode(Float64Array dst) throws IOException {
        long newLen = buf.readUvarintCompact();
        if (newLen < 0) {
            throw new IllegalStateException("Invalid array length: " + newLen);
        }
        if (newLen > Integer.MAX_VALUE) {
            throw new IllegalStateException("Array length exceeds maximum: " + newLen);
        }

        dst.ensureLen((int)newLen);
        for (int i = 0; i < newLen; i++) {
            dst.elems[i] = elemDecoder.decode();
        }

        return dst;
    }
}
