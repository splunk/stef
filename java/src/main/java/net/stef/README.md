# Java runtime core

Core STEF reader/writer implementation used by generated bindings and tests.

## What lives here

- Frame encoding/decoding
- Codecs for built-in data types
- Various helpers and limits

## Developing

- Build and test via the top-level Gradle build (`./gradlew build`).

